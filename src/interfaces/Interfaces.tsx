export interface Intervention {
    name: string | string[]
    location: string | string[]
    leader: string | string[]
    completed: boolean
}
export interface IEmailId {
    name: string
    id: string
}
